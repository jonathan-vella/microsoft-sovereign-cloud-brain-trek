# Astro / Starlight build artifacts
node_modules/
dist/
.astro/
*.log
.env
.env.local
.DS_Store

# Migration script working artifacts.
# `migration-report.json` is per-run diagnostic output; `path-rewrite-map.json`
# is committed (post-Phase-6, it is regenerated by `scripts/rebuild-path-rewrite-map.mjs`
# from the built `dist/` tree, not from the deleted `docs/` source).
needs-manual-fix/
migration-report.json
