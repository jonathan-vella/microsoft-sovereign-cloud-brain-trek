---
/**
 * ThemeSelect.astro
 *
 * Overrides Starlight's built-in theme switcher with an empty component so
 * the selector is hidden from the site header. The site is locked to light
 * theme (see `ThemeProvider.astro`).
 */
---
