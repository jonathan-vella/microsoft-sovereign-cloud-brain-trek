{
  "_comment": "pa11y-ci config — Plan §0.1 + §4.3. URLs are populated by run-audit.sh from inventory.csv. Run against locally-built site (default port 4000 with `bundle exec jekyll serve`, or any local static server pointing at _site/).",
  "defaults": {
    "standard": "WCAG2AA",
    "timeout": 60000,
    "wait": 1000,
    "ignore": [
      "warning",
      "notice"
    ],
    "chromeLaunchConfig": {
      "args": [
        "--no-sandbox",
        "--disable-dev-shm-usage"
      ]
    }
  },
  "urls": []
}
